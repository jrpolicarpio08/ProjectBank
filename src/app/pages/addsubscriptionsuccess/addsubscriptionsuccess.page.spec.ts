import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddsubscriptionsuccessPage } from './addsubscriptionsuccess.page';

describe('AddsubscriptionsuccessPage', () => {
  let component: AddsubscriptionsuccessPage;
  let fixture: ComponentFixture<AddsubscriptionsuccessPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AddsubscriptionsuccessPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
