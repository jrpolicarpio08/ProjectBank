import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeleteaccountsuccessPage } from './deleteaccountsuccess.page';

describe('DeleteaccountsuccessPage', () => {
  let component: DeleteaccountsuccessPage;
  let fixture: ComponentFixture<DeleteaccountsuccessPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DeleteaccountsuccessPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
