import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LockaccountsuccessPage } from './lockaccountsuccess.page';

describe('LockaccountsuccessPage', () => {
  let component: LockaccountsuccessPage;
  let fixture: ComponentFixture<LockaccountsuccessPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LockaccountsuccessPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
