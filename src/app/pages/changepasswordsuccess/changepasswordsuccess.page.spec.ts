import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChangepasswordsuccessPage } from './changepasswordsuccess.page';

describe('ChangepasswordsuccessPage', () => {
  let component: ChangepasswordsuccessPage;
  let fixture: ComponentFixture<ChangepasswordsuccessPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ChangepasswordsuccessPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
