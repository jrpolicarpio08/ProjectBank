import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditprofilesuccessPage } from './editprofilesuccess.page';

describe('EditprofilesuccessPage', () => {
  let component: EditprofilesuccessPage;
  let fixture: ComponentFixture<EditprofilesuccessPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditprofilesuccessPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
